# Kickoff Report - VIC/Aura

Date: 2026-05-03  
Working copy: `C:\Users\ghost\PROJECTS\codexEdit_VICv1`  
Selected source: `C:\Users\ghost\PROJECTS\OMNI_VIC-v1.2`

## Executive Status

VIC/Aura now has a clean `codexEdit_` working folder based on the strongest continuation candidate. The selected source has the modern Electron/Vite/React HUD, backend services, agent behavior files, voice/navigation/OBD-oriented pieces, setup docs, and tests. Original project folders were not edited.

The next project phase should focus on making the app reliably runnable, then tightening the driving-critical surfaces: voice, navigation, vehicle telemetry, startup, and safety gating.

## Source Selection Notes

`OMNI_VIC-v1.2` was chosen over the older `VIC` archive because it has:

- A cleaner project root with `src`, `electron`, `backend`, `vic-agent`, `skills`, `tools`, and `test`.
- Backend modules for ADA, authentication, CAD, Kasa, printer, project manager, server, tools, and web agent.
- A stronger documentation set: `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `FEATURES.md`, `PROGRESS.md`, `QUICKSTART.md`.
- More complete Aura/VIC-facing dependencies, including Socket.IO, Three.js, React Three Fiber, MediaPipe, and Electron tooling.

Reference candidates to mine later:

- `C:\Users\ghost\PROJECTS\VIC\VIC_v8.0` - newest in the older VIC archive.
- `C:\Users\ghost\PROJECTS\OMNI_VIC-v1\OMNI_VIC-v1.1\ACTION_ITEMS.md` - latest security action notes from the prior OMNI tree.

## Copy Scope

Created `C:\Users\ghost\PROJECTS\codexEdit_VICv1` from `OMNI_VIC-v1.2`.

Excluded during copy:

- `.git`
- `node_modules`
- `dist`
- `venv`
- `__pycache__`
- `.pytest_cache`
- `.chrome-profile`
- `.env`
- `client_secret_*`
- `*.pyc`
- local dev-server logs

This keeps the working copy lighter and reduces credential leakage risk.

## Immediate Risks

1. Prior action notes mention exposed Google/Gemini/Maps/OAuth credentials in older folders. Those keys should be treated as compromised until rotated.
2. The copied working folder does not have Git metadata yet, so there is no local commit baseline.
3. `node_modules` was intentionally excluded, so install/build verification still needs to run.
4. Native Electron modules can require Visual Studio Build Tools 2022 on Windows.
5. Multiple historical VIC folders may contain useful changes, but they must be compared deliberately instead of merged by timestamp alone.

## Secret Scan Update

The initial copied `.env.example` contained credential-shaped values. It was replaced with placeholder values, and a follow-up PowerShell scan reported no secret-pattern hits in scanned text files. Older source folders may still contain compromised credentials and should be handled separately.

## Bring-Up Verification

Completed in `C:\Users\ghost\PROJECTS\codexEdit_VICv1`:

- `npm install` passed after aligning `react`, `react-dom`, `@types/react`, and `@types/react-dom` to React 18-compatible versions for `@react-three/drei@9.x`.
- `npm run test:setup` passed.
- `npm run typecheck` passed after removing unused React imports and unused destructured props.
- `npm run build` passed.
- `npm run dev:web` is running at `http://127.0.0.1:5173`; an HTTP check returned status 200 with an HTML title.

Known verification gap: Electron shell smoke-test has not been run yet.

Known dependency risk: npm reports 8 audit vulnerabilities after install. `npm audit fix` did not resolve them without major upgrades; the suggested fixes include `electron@41.5.0` and `@electron/rebuild@4.0.4`, so this should be handled as a planned compatibility task rather than a forced update.

## Next Action Recommendation

Run this sequence in the working copy:

1. Initialize local Git in `codexEdit_VICv1`.
2. Start `npm run dev` for Electron shell verification.
3. Inspect `npm audit` output and choose targeted dependency fixes.
4. Verify backend startup.
5. Verify voice simulation, navigation degraded mode, and OBD unavailable behavior.
6. Add focused fixes only after the failure surface is known.
